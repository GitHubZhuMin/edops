# EdOps（zhjx）统一部署与配置管理 PRD（本期）

版本：v0.1
日期：2026-02-04

## 背景与问题
EdOps 面向 zhjx 体系提供统一部署与配置管理能力，但当前部署路径分散、配置不一致、交付成本高。需要在不引入新配置机制、不改变 Tutor 结构的前提下，形成可复制、可审计、低门槛的交付流程。

## 目标
- 提供统一的部署与配置工作流，降低交付实施的复杂度。
- 以单一 `config.yml` 管控配置，以 `RUN_ZHJX_*` 控制模块启停。
- 聚焦 `local` 与 `portainer` 两种部署模式，K8s 本期屏蔽。
- 支持本地 Docker 与私有镜像仓库场景，满足内网/离线交付需求。
- 模块依赖为弱依赖，只做提示不做强制阻断。

## 目标用户
- 主用户：交付实施人员
- 次用户：研发自测人员

## 核心场景
- 首次部署：交付人员完成配置与一键部署。
- 日常运维：启停、状态、日志、健康检查。
- 版本升级与回滚：镜像版本管理、历史记录与回退。
- 模块启停：通过 `RUN_ZHJX_*` 切换业务模块。
- 环境迁移：通过统一配置与可复用命令快速复制环境。

## 范围
- 部署模式：`local`、`portainer`
- 配置管理：`config save/get/list/validate` 等核心命令
- 镜像管理：版本查询、拉取、检查与回滚
- 模块管理：基础模块默认开启，业务模块可选启用

## 非目标
- 不支持 K8s 交付与命令链路（本期屏蔽）
- 不引入多环境配置体系（dev/beta/prod）
- 不做源码构建，镜像产出仍由 Jenkins 负责
- 不改变 Tutor/edx 上游结构与部署路径

## 关键需求
- 统一配置源：所有配置集中于 `config.yml`
- 模块启用机制：仅允许 `RUN_ZHJX_*` 作为唯一开关
- 部署命令链路：local 支持 `launch/start/stop/restart/status/logs/healthcheck/history/rollback`，portainer 提供模板渲染与部署命令的最小闭环
- 镜像管理：支持私有 registry、离线环境可用
- 依赖提示：对模块依赖进行提示性校验，不强制中断

## 约束与假设
- 镜像由 Jenkins 构建与推送，EdOps 仅消费与管理镜像
- 本期以交付效率优先，功能设计保持低门槛
- 配置覆盖通过 `--root` 或修改 `config.yml` 实现

## 验收标准
- 交付人员可在单机环境 1 次配置完成部署启动
- 私有 registry 场景下可拉取并部署镜像
- 通过命令可完成版本升级与回滚
- 模块依赖缺失时有明确提示
- 文档与 CLI 行为保持一致

## 风险与缓解
- 依赖提示不阻断可能导致运行异常
- 端口/证书/路径差异导致部署失败
- 缓解：在 `validate` 与 `launch` 中输出清晰提示
